# AI 共创场

一个面向 3–12 人现场共创活动的轻量网页工具。它把候场、两轮作答、匿名互评、自由交流、公布结果与报告导出放在同一条简洁流程里。

项目默认不绑定任何大模型账户，也不要求 API Key。

## 适合什么场景

- 团队 AI 共创、Prompt 挑战或工作坊
- 3–12 人、1–2 轮的现场活动
- 主持人手动控时，希望参赛者通过链接直接加入
- 希望先用互评形成结果，再按需加入外部 AI 评分

## 一场活动如何进行

1. 组织者创建活动，保存主持人、参赛者、大屏和报告入口。
2. 参赛者通过活动链接和房间码加入候场；同一浏览器可自动恢复席位。
3. 主持人播放浏览器内置语音开场或复制开场词人工介绍，然后锁定名单。
4. 参赛者完成 V1、一次追加 Prompt 与 V2；每次阶段结束后内容锁定。
5. 所有人匿名投出最喜欢的作品，不能投给自己。
6. 主持人可直接按互评票数公布结果；自由交流由主持人手动结束。
7. 如需补充评分，可一键复制或下载整轮匿名作品，交给任意自有 AI 工具评分，再把 0–10 的分数回填。
8. 活动结束后下载整场 JSON 报告，包含题目、作品、投票、可选外部评分和总成绩。

默认规则是：**单轮成绩 = 获得的互评票数**。只有主持人主动回填外部评分时，单轮成绩才变为“外部评分 + 互评票数”。

## 本地运行

需要 Node.js `>=22.13.0`。

```bash
npm ci
npm run dev
```

本项目使用 Cloudflare D1 保存活动数据。首次本地运行前，请将 `drizzle/` 中的迁移应用到本地模拟数据库，并通过私密方式配置一次性创建邀请码。

## 校验

```bash
npm test
npm run lint
```

`npm test` 会依次运行核心活动测试、生产构建和页面渲染检查。

## 部署与数据边界

- 部署时需要为运行环境提供 D1 数据库绑定 `DB`。
- `.openai/hosting.json` 中的项目 ID 只是占位符；请替换为自己的部署项目，不要复用他人的配置。
- 不要把 `.env`、真实邀请码、活动链接、房间码或参会者导出数据提交到仓库。
- 项目不内置任何模型供应商或 API Key；外部 AI 评分完全由活动使用者自行选择和执行。

更详细的现场主持步骤见 [docs/LIVE-RUNBOOK.md](docs/LIVE-RUNBOOK.md)。

## 项目结构

- `app/`：参赛者、主持人、大屏和报告页面，以及 API 路由
- `src/`：活动、候场、现场阶段与评分规则
- `db/` 与 `drizzle/`：D1 数据库结构和迁移
- `tests/`：核心流程与页面渲染测试
- `docs/adr/`：关键产品与技术决策

## 许可证

本项目采用 [MIT License](LICENSE)。
